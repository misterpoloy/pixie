{
  "framework": "nextjs",
  "buildCommand": "yarn build",
  "installCommand": "yarn install --frozen-lockfile",
  "env": {
    "DATABASE_URL": "@pixie-database-url",
    "NEXTAUTH_SECRET": "@pixie-nextauth-secret",
    "NEXTAUTH_URL": "@pixie-nextauth-url"
  }
}
